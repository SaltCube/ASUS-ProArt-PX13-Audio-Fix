.DS_Store
*.swp
*.swo
*~
temp/
PX-13-audio-context-compact.txt

# Kernel module build output: the install steps build in place, in src/tas2783/
*.ko
*.o
*.mod
*.mod.c
*.cmd
.*.cmd
Module.symvers
modules.order

# The ASUS installer and the firmware extracted from it: redistribution is not
# ours to make, and install.sh downloads the installer into the repo root.
firmware/
*.exe

# Local machine dump, useful while working but not part of the fix
specs.txt
